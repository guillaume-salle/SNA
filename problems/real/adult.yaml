dataset: adult
dataset_params:
  test_size: 0.33 # Standard split for adult dataset
  val_size: 0.0
  param_dim: 106

log_test_every_n_batches: 20
log_train_every_n_batches: 20

optimal_lr: "expr: float(2e-10)"

model: logistic_regression
model_params:
  bias: true
  lambda_: 1e-4 