dataset: linear_regression
dataset_params:
  n_dataset: 1e4
  true_theta: [0.0, 0.0]
  bias: &bias_setting False 
  cov_type: "hard" 
  cov_const: 100.0
  # diag:
model: LinearRegression
model_params:
  bias: *bias_setting
radius: 1.0